[package]
name = "casclib-sys"
version.workspace = true
edition.workspace = true
license = "MIT"
authors.workspace = true
links = "casc"
description = "Hand-written FFI bindings to Ladislav Zezula's CascLib (vendored, built from source)."

[lib]
doctest = false

[build-dependencies]
cc = "1"
